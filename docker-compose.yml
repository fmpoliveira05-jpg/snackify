# MongoDB local para desenvolvimento: `docker compose up -d`
# Depois, no backend/.env: MONGODB_URI=mongodb://localhost:27017/snackify
services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
